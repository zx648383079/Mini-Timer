{
  "name": "mini_timer",
  "version": "1.0.0",
  "scripts": {
    "start": "gulp && gulp vue",
    "watch": "gulp && gulp vue && gulp watch",
    "build": "gulp cleanall && gulp && gulp vue && gulp cleantmp"
  },
  "devDependencies": {
    "@types/node": "^15.0.0",
    "gulp": "^4.0.2",
    "gulp-clean": "^0.4.0",
    "gulp-rename": "^2.0.0",
    "gulp-replace": "^1.1.1",
    "gulp-sass": "^4.1.0",
    "gulp-typescript": "^5.0.1",
    "gulp-vue2mini": "^1.1.15",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "ts-md5": "^1.2.7"
  }
}
