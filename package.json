{
  "name": "mini_timer",
  "version": "1.0.0",
  "scripts": {
    "start": "gulp && gulp vue",
    "watch": "gulp && gulp vue && gulp watch",
    "build": "gulp cleanall && gulp && gulp vue && gulp cleantmp"
  },
  "devDependencies": {
    "@types/node": "^13.13.12",
    "gulp": "^4.0.2",
    "gulp-clean": "^0.4.0",
    "gulp-rename": "^2.0.0",
    "gulp-replace": "^1.0.0",
    "gulp-sass": "^4.1.0",
    "gulp-typescript": "^5.0.1",
    "gulp-vue2mini": "^1.0.6",
    "node-sass": "^4.14.1",
    "typescript": "^3.9.5"
  },
  "dependencies": {
    "ts-md5": "^1.2.7"
  }
}
